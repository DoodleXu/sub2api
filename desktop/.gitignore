# The root repository ignores every dist/ directory. The checked-in marker is
# needed so `go test ./...` can embed assets before Vite runs; the actual Vite
# output is generated in CI and remains ignored.
!frontend/
!frontend/dist/
!frontend/dist/.gitkeep
